const schoolsData = require( './schools.json' )
const seasonsData = require( './seasons.json' )
const gamesData = require( './games.json' )

console.log( schoolsData[ 0 ] )

module.exports = {
  schoolsData,
  seasonsData,
  gamesData
}